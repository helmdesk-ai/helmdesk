<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

class DatabaseSeeder extends Seeder
{
    /**
     * Seed the application's database.
     *
     * AI 内置供应商在系统创建时由 SeedSystemAiProvidersAction 自动种入，
     * 这里只保留全局示例数据所需的种子流程。
     */
    public function run(): void
    {
        //
    }
}
